


function sumar (){
    var numero1=  parseInt(prompt("Ingrese el primer numero:",1));
    var numero2=  parseInt(prompt("Ingrese el segundo numero:",1));
    var result = null;
    result = numero1 + numero2
    alert ("El resultado de la suma es: " + result);    
}  

function restar (parametro){
    var numero1=  parseInt(prompt("Ingrese el primer numero:",1));
    var numero2=  parseInt(prompt("Ingrese el segundo numero:",1));
    var result = null;
    result = numero1 - numero2
    alert ("El resultado de la resta es: " + result);  
}  

function multiplicar (parametro){
    var numero1=  parseInt(prompt("Ingrese el primer numero:",1));
    var numero2=  parseInt(prompt("Ingrese el segundo numero:",1));
    var result = null;
    result = numero1 * numero2
    alert ("El resultado de la multiplicacion es: " + result);
}  

function dividir (parametro){
    var numero1=  parseInt(prompt("Ingrese el primer numero:",1));
    var numero2=  parseInt(prompt("Ingrese el segundo numero:",1));
    var result = null;
    result = numero1 / numero2
    alert ("El resultado de la division es: " + result);
}  
